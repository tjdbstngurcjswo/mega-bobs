export const titleBarStyle = {
  background: 'rgba(255,255,255,0.72)',
  backdropFilter: 'blur(12px)',
  boxShadow: '0 1px 0 rgba(0,0,0,0.06)',
} as const;

export const trafficBtnClass =
  'size-3 rounded-full flex items-center justify-center cursor-pointer focus-visible:outline-none';

export const viewToggleBtnClass =
  'flex items-center gap-1 text-[11px] text-muted hover:text-ink cursor-pointer transition-colors px-2 py-0.5';
